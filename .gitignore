# Binaries
server
*.exe
*.exe~
*.dll
*.so
*.dylib

# Go build/test artifacts
*.test
*.out
coverage.out
coverage.html

# Local env (keep .env.example committed if you add one)
.env
.env.local
.env.*.local

# Dependency/vendor directories (optional)
vendor/

# Frontend (React / Vite / etc.)
node_modules/
dist/
build/
.next/
out/

# Local Docker overrides (secrets, port tweaks)
docker-compose.override.yml

# IDE / OS
.DS_Store
.idea/
.vscode/
*.swp
*.swo
